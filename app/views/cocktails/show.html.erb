<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <a class="navbar-brand" href="#">
    <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/logo.png" />
</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to"home", root_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Messages</a>
      </li>
      <li class="nav-item dropdown">
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="#">Action</a>
          <a class="dropdown-item" href="#">Another action</a>
        </div>
      </li>
    </ul>
  </div>
</div>

<div class="show-banner">
</div>

<div class="container">

    <div class="ingredient-card" style="width: fit-content;">
      <div class="cocktail-name"><h1><%= @cocktail.name %></h1>
        <div class="cocktail-image">
          <% if @cocktail.photo.attached? %>
            <%=cl_image_tag @cocktail.photo.key, height: 300, width: 550, crop: :fill%>
          <% else  %>
          <% end %>
          <div class="dose-details"> <% @cocktail.doses.each do |dose| %>
            <li><%= dose.description %>
            <%= dose.ingredient.name %>
            <button><%= link_to 'Delete ingredient', cocktail_dose_path(@cocktail, dose.id), method: :delete,
            data: { confirm: "Are you sure you want to delete this ingredient?"} %></button></li>
            <% end  %>
                <button><%= link_to "add ingredients", new_cocktail_dose_path(@cocktail.id)  %></button>
                <button><%= link_to 'Delete Cocktail', cocktail_path(@cocktail), method: :delete,
                data: { confirm: "Are you sure you want to delete this Cocktail?"}%></button>
          </div>
        </div>
      </div>
    </div>
</div>


<button><%= link_to "back", cocktails_path  %></button>



